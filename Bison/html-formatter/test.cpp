

int main() { /* com */
  int a;/* com */
  int a = {{1,a(),a},2,3};/* com */
  a = 1+1+1*2*2*2+5+55+5;/* com */
  int b = 2 * a;/* com */
  print(a,b);/* com */
}/* com */


int bar(int a, int b) {/* com */
  
  a = -10 + 2*2*22+5;/* com */
  switch (a){/* com */a;/* com */
  }/* com */
  if(a)a;/* com */
  
  if(a){a;/* com */}/* com */
  
  switch (a){/* com */
    case 'q':ok(a);ok(a);ok(a);ok(a);ok(a);ok(a);ok(a);
    break;
    case 'w':{nok('w');return true;}/* com */
    default: return a();/* com */
  }/* com */
  int a = {1,2,{/* com */1,2,3}/* com */,{/* com */a[3],e,r,t,y}/* com */}/* com */;
  
  if(a)a;else b;
  
  if(a){a;}else b;
  
  if(a){a;}else{b;}
  
  if(a)a;else{b;}
  
  if(a)if(a)if(a)if(a)if(a)b; else a;else a;else a;else a;
  
  if(a)a();else{}
  
  if(a)if(b)if(c)f();else {d();} else e();
  
  if(a) {if (b) if (c) d(); else {} else {d();}}
  
  while (a) while(b)while(c){while(d)while(f)ff(f);}
  while(a + b * c) {
    c = a*b*c;
  }
  do {
    a += 1;
  } while(true);
  return a(a(1,1,1,b(2),c(),1),s(),s);
  
  for(i = 0; i < 10; i += 10)
  for(i = 0; i < 10; i += 10)
  for(i = 0; i < 10; i += 10)
  for(i = 0; i < 10; i += 10){}
  
  for(i = 0; i < 10; i += 10){
    if (a == i) {
      print("123", a, a * a, a[i + 10 * i*i*i+10] = 0);
    }
    if (a != i) {
      print("123", a, a * a);
    } else {
      a(0);
    }
    
  }
  if (a) if (b) a(); else b();
  
  for(i = 0; ; i += 10)
    a +=1;
    
    
  {a;r;t;y;u;}
  
  a += 10;
}

unsigned char long fooo(char a="12356", const int b=0, double c=10);

int a,b,q,w,e;
char c='1',d,e,f='a';
  
unsigned long long foo(char a, const int b, double c);

long double a[1000];